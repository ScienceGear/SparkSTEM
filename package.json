{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run build -w backend && concurrently -n \"frontend,backend\" -c \"cyan,green\" \"npm run dev -w frontend\" \"npm run start -w backend\"",
    "dev": "concurrently -n \"frontend,backend\" -c \"cyan,green\" \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build --workspaces --if-present",
    "build:backend": "npm run build -w backend",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^8.0.0",
    "prettier": "^3.8.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "openai": "^6.32.0"
  }
}
